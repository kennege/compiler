func exp(float32 num, float32 power)
{
    n := 1
    for (i:=0; i<power; i++)
    {
        n = n * num;
    }   
    return n
}

func main() int
{
    pi := 0.0
    for (i:=0; i<1000; i++)
    {
        pi = pi + (exp(-1.0, i) / ((2.0 * i) + 1.0))
    }

    return 4 * pi
}