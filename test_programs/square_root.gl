// my first goLite program

func root(float32 input)
{
    i := 1.0
    
    for (j:=0; i*i<d; j++)
    {
        i = i + 1.0
    }
    p := i - 1.0
    x := 1.0
    for (i:=0; i<10; i++)
    {
        x =  p + (d-p*p) / (p + x)
    }
    return x
}

func main() int
{
    d := 25.0
    r := root(d)     
    return r               
}
